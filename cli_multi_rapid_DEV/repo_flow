#!/usr/bin/env bash
set -euo pipefail

if [ -x ".det-tools/scripts/repo_flow.sh" ]; then
  exec ./.det-tools/scripts/repo_flow.sh "$@"
else
  echo "Missing .det-tools/scripts/repo_flow.sh"
  exit 1
fi

